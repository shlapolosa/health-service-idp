# DEPRECATED (#163 SUBSTRATE-GITOPS): knative-service.yaml moved to
# factory/substrate/services/slack-api-server/ and is synced by the ArgoCD
# Application "substrate-services". This kustomization no longer builds
# (kustomize cannot reference parent paths); kept only as a record of the
# old multi-registry image-override pattern.
# Kustomization for Slack API Server with multi-registry support
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources: []

# Comment/uncomment based on target registry
# For Docker Hub deployment:
# images:
# - name: healthidpuaeacr.azurecr.io/slack-api-server
#   newName: docker.io/socrates12345/slack-api-server
#   newTag: latest

# For ACR deployment (current):
images:
- name: healthidpuaeacr.azurecr.io/slack-api-server
  newTag: oam-driven-v1

# Remove imagePullSecrets for Docker Hub
# patches:
# - target:
#     kind: Service
#     name: slack-api-server
#   patch: |-
#     - op: remove
#       path: /spec/template/spec/imagePullSecrets