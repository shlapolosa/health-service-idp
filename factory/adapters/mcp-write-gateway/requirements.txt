# Capability Factory MCP — pinned deps. Verify latest with context7 before bumping.
mcp>=1.9.0                       # MCP Python SDK (FastMCP + Streamable-HTTP)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
starlette>=0.40.0
requests>=2.32.0
pyjwt[crypto]>=2.9.0             # RS256 sign GitHub App JWT + Entra JWT validation
cryptography>=43.0.0
# Workload Identity → Key Vault for the GitHub App PEM (prod path).
azure-identity>=1.19.0
azure-keyvault-secrets>=4.9.0
