# RASA-CONTAINER (#178): invariant channel credentials fallback (REST only —
# no secrets here; channel tokens belong in envFrom secrets, never in files).
# A bot may override by shipping its own credentials.yml in /app/bot/.
rest:
