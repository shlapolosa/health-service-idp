sync:
  toHost:
    pods:
      enabled: true
    ingresses:
      enabled: true
    persistentVolumeClaims:
      enabled: true
    persistentVolumes:
      enabled: true
    storageClasses:
      enabled: true

controlPlane:
  coredns:
    enabled: true
    embedded: false
  statefulSet:
    scheduling:
      nodeSelector:
        alpha.eksctl.io/nodegroup-name: socrateshlapolosa-karpenter-demo-ng-medium
    nodeSelector:
      alpha.eksctl.io/nodegroup-name: socrateshlapolosa-karpenter-demo-ng-medium
