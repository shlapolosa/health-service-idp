# Test OAM Application for Healthcare Identity Service
apiVersion: core.oam.dev/v1beta1
kind: Application
metadata:
  name: clinic-identity-test
  namespace: default
spec:
  components:
    - name: clinic-identity
      type: identity-service
      properties:
        domain: healthcare