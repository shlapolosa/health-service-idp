apiVersion: v1
kind: ConfigMap
metadata:
  name: simple-api-gateway-gateway-config
  namespace: default
  labels:
    app.kubernetes.io/component: graphql-gateway
    app.kubernetes.io/name: simple-api-gateway
data:
  config.yaml: |
    # GraphQL Gateway Configuration
    gateway:
      name: "simple-api-gateway"
      autoDiscovery: false
      refreshInterval: "5m"
    serviceDiscovery:
      selector:
        app.kubernetes.io/managed-by: kubevela
      namespaces: ["default"]
    cors:
      enabled: true
      origin: "*"
    playground:
      enabled: true